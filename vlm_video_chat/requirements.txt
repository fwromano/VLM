# VLM Camera Requirements
transformers>=4.37.0
torch>=2.0.0
torchvision>=0.15.0
accelerate>=0.20.0
opencv-python>=4.8.0
pillow>=9.0.0
bitsandbytes>=0.40.0
sentencepiece>=0.1.99
einops>=0.6.0
timm>=0.9.0
# flash-attn>=2.3.0  # Optional: For performance optimization (requires CUDA dev tools)

# For advanced multi-threaded GUI version
# tkinter (usually included with Python)
# Note: On Ubuntu/Debian install with: sudo apt-get install python3-tk